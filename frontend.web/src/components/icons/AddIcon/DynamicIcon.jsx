import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`i6f8660f49748427c96abc01d7ca544b5-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
