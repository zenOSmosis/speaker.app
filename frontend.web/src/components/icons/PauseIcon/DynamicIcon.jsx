import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`i3c329622db814cebbbde1566266d2926-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
