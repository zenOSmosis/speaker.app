import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`ifc5d4d1462994a799ec8ad933c453adc-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
