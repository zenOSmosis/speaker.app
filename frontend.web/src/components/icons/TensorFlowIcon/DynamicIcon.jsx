import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`i4d8c5e15866b4a5987ec9303aeb514b5-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
