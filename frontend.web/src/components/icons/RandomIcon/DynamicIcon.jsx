import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`i1f364d0354ff4a9abe03a6a2cacee4bf-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
