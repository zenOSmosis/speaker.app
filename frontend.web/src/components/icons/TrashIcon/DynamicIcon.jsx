import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`ic450a4e02c344d67bcb1f5a2e4a23855-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
