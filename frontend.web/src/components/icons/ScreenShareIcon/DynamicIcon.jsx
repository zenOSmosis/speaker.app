import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`i2ff96f2b4d0e43b4a9c2b2b80abfa76e-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
