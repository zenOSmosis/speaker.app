import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`if77f1d769a8d4bee95cfa2ef4b9bffb3-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
