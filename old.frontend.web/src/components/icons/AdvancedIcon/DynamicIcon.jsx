import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`i577e780d5f7a4e789a2fcd8b42eae7aa-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
