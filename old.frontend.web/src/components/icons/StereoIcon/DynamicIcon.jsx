import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`i5f649e485f634efc93252cd5a55b299d-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
