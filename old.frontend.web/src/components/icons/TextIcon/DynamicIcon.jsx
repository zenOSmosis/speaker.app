import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`id2bc3b576a714b528ce52a471f707177-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
