import React from 'react';
import './style.css';

/**
 * Dynamically generated ReactComponent.
 */ 
const DynamicIcon = ({ className, ...propsRest }) => {
  return (
    <i
      {...propsRest}
      className={`ic22199983f4a465aa2a5acf6a646319b-icon${className ? ` ${className}` : ''}`}
    />
  );
};

export default DynamicIcon;
